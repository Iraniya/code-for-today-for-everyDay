#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <stdio.h>

using namespace std;

int insert(int x,int arr){


}

int main() {
    int n,q,x,y,num;
    std::vector<int> arr(10);
    cin>>n;
    cin>>q;
    for(int i=0;i<q;i++){
        scanf("%d %d %d",&num,&x,&y);
    }

    if(num==1){
        insert(int y,arr);
    }

    return 0;
}
